import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Checkbox from './Button.vue';

<Meta title="MDX/Checkbox" component={Checkbox} />

export function mdx1 (args, { argTypes }) {
    return {
        props: Object.keys(argTypes),
        components: {Checkbox},
        template: '<Checkbox v-bind="$props" />',
    }
};

<Story
    name="positive"
    args={{
        status: 'positive',
        label: 'Positive',
    }}>
    {mdx1.bind({})}
</Story>